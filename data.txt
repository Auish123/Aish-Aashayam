 public ActionResult registrateUser()
        {
            var namedata = new List<UserData>();
            using (MyDataBaseEntities db = new MyDataBaseEntities())
            {
                namedata = db.UserDatas.ToList();
            }
            return View(namedata);
        }




@model List<finalMvc.Models.UserData>

@{
    ViewBag.Title = "registrateUser";
    var gridview = new WebGrid(source: Model, canPage: true, rowsPerPage: 5);
    gridview.Pager(WebGridPagerModes.All);
}

<h2>registrateUser</h2>

<div class="container">
   @gridview.GetHtml(columns:gridview.Columns(
    gridview.Column(columnName: "Id", header: "UserID"),
    gridview.Column(columnName: "FirstName", header: "Firstname"),
    gridview.Column(columnName: "LastName", header: "Lastname"),
    gridview.Column(columnName: "UserName", header: "User name"),
    gridview.Column(columnName: "Password", header: "Password"),
    gridview.Column(header: "Delete", format: @<text><a href="#">Delete</a></text>)
            ))
</div>
